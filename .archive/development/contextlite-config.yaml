# ContextLite Configuration for rustchain-community
server:
  port: 8099
  host: "127.0.0.1"
  cors_enabled: false
  
storage:
  database_path: "C:/Users/micha/repos/rustchain-community/contextlite.db"
  cache_size_mb: 64
  
smt:
  solver_timeout_ms: 250
  max_opt_gap: 0.05
  
logging:
  level: "info"
  include_timings: true

# Project-specific settings
project:
  name: "rustchain-community"
  path: "C:/Users/micha/repos/rustchain-community"
  
# Multi-workspace configuration  
cluster:
  enabled: true
  node_id: "rustchain-community-node"
  discovery:
    method: "static"
    service_name: "rustchain-community"
