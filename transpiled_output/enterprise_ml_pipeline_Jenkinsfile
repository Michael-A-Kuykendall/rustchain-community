pipeline {
    agent any

    stages {
        stage('Enterprise Vector Store: Pinecone') {
            steps {
                sh 'echo \'Step type RagAdd not yet supported in shell export\''
            }
        }
    }

    post {
        always {
            echo 'RustChain mission execution completed'
        }
        success {
            echo 'Mission completed successfully'
        }
        failure {
            echo 'Mission failed'
        }
    }
}
