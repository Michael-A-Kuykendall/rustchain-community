# Generated from RustChain mission: langchain_mission
# Kubernetes Job manifest
---
apiVersion: batch/v1
kind: Job
metadata:
  name: langchain-mission
  labels:
    app: rustchain-mission
    mission: langchain-mission
spec:
  template:
    spec:
      restartPolicy: OnFailure
      containers:
      - name: rustchain-executor
        image: rustchain/executor:latest
        command: ["/bin/sh"]
        args:
        - -c
        - |
          echo "Executing step: Enterprise Vector Store: Pinecone"
          echo 'Step type RagAdd not yet supported in shell export'
        env:
